
#include <ld_log.h>

#include "ld_bitset.h"

int main() {
    // struct timespec res;
    // clock_getres(CLOCK_MONOTONIC, &res);
    // printf("Resolution: %ld.%09ld seconds\n", res.tv_sec, res.tv_nsec);
    log_warn("?????????????? %d", time(NULL));
}
